<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.financials.starwars">

    <uses-permission tools:node="merge" android:name="android.permission.INTERNET"/>
    <uses-permission tools:node="merge" android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".StarWarsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StarWars">
        <activity
            android:name=".MainActivity"
            tools:node="replace"
            tools:replace="android:launchMode"
            android:launchMode="standard"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>